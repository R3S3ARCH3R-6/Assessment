package ex2;

import java.io.FileInputStream;	//used to read in files
import java.io.FileNotFoundException;

public class Logic {
	/*public void openFile(String filename){
		FileInputStream fis = new FileInputStram(filename);
			//FileInputStream - used to open files
			//we are in the red b/c we have a checked exception b/c we need to handle if we can't read the file
	}*/
	
	//correct version/one that throws an exception
	//if we didn't include "throws FileNotFoundException", the program won't compile successfully b/c it is a checked exception (knows it has a high degree of causing errors)
	
	public void openFile(String filename) throws FileNotFoundException{
		FileInputStream fis = new FileInputStream(filename);
			//FileInputStream - used to open files
			//we are in the red b/c we have a checked exception b/c we need to handle if we can't read the file
	}
}
